name: release

# Build and sign the helper, notarize and staple it, build the interposer for
# the simulator slice, attach both to a GitHub release, and update the Homebrew
# cask. Implemented in M5.

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Placeholder. Implement sign, notarize, and package in M5."
