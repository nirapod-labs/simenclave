# docs

User-facing docs: how to integrate (use the `SecKey` C API, set the scheme env), install, the protocol, the security model, and troubleshooting.

The design itself, the decisions and the architecture, lives in the `nirapod-arch` repo, not here. Built out in M5.
